[build]
  command = "npm --prefix dressme ci ; npm --prefix dressme run build"
  publish = "dressme/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
